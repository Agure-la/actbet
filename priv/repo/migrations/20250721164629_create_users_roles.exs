defmodule Actbet.Repo.Migrations.CreateUsersRoles do
  use Ecto.Migration

  def change do
  #  create table(:users_roles, primary_key: false) do
    #  add :user_id, :bigint, null: false
    #  add :role_id, :bigint, null: false

    #  timestamps()
    #end

    #create unique_index(:users_roles, [:user_id, :role_id])

   
  end
end
